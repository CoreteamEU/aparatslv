{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Local Site",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Run Local Server"
        }
    ]
}